{% extends 'base.html.twig' %}

{% block body %}
    <div class="container p-5">
        <div class="row">
            <div class="col">
                <img class="img-fluid" src="{{ asset('img/pizzas/' ~ pizza.image) }}">
            </div>
            <div class="col">
                <h5>
                    {{ pizza.size }}
                    {{ pizza.name }}
                </h5>
                <h6>
                    €{{ pizza.price }}
                </h6>
                <p>{{ pizza.description }}</p>
                <a href="{{ path('order', {id: pizza.id}) }}" type="button" class="btn btn-success">Bestellen</a>
            </div>
        </div>
    </div>
{% endblock %}
